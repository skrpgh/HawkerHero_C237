<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f7f7f7;
            color: #333;
        }
        .container {
            margin-top: 50px;
        }
        .dashboard-header {
            margin-bottom: 30px;
        }
        .btn-custom {
            background-color: #f0ad4e;
            color: white;
            font-weight: bold;
        }
        .btn-custom:hover {
            background-color: #ec971f;
        }
    </style>
</head>
<body>

    <div class="container">
        <div class="dashboard-header text-center">
            <h1>Welcome, <%= user.username %>!</h1>
        </div>

        <% if (user.role === 'admin') { %>
            <div class="admin-dashboard">
                <h2 class="text-center">Admin Dashboard</h2>
                <p class="text-center">You can manage hawker stalls here.</p>
                <div class="text-center">
                    <a href="/admin" class="btn btn-custom btn-lg">Go to Admin Panel</a>
                </div>
            </div>
        <% } else { %>
            <div class="user-dashboard">
                <h2 class="text-center">User Dashboard</h2>
                <p class="text-center">You have limited access. You can view and search for hawker stalls.</p>
                <div class="text-center">
                    <a href="/view-stalls" class="btn btn-primary btn-lg">View Stalls</a>
                </div>
            </div>
        <% } %>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.3/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>


