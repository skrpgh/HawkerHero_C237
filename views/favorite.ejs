<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>Hawker Hero App</title>
</head>
<body>
  <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">Hawker Hero App</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="collapsibleNavbar">
        <ul class="navbar-nav">
          <li class="nav-item">
            <a class="nav-link" href="/addFavorite">Add new favorite</a>
          </li> 
        </ul>
      </div>
    </div>
  </nav>
  
  <div class="container">
    <br>
    <div class="text-center"><h2>Favorite Details</h2></div>
    <br>
    <% if (favorite) { %> <!-- Check if favorite data exists -->
      <div>
        <p><b>Username:</b><br> <%= favorites.username %></p>
        <p><b>Stall:</b><br> <%= favorites.stall %></p>
        <p><b>Food:</b><br> <%= favorites.food %></p>
        <p><b>Notes:</b><br> <%= Favorites.notes %></p>
        <p><b>Created At:</b><br>
          <% if (favorites.created_at) { %>
            <%= new Date(favorites.created_at).toISOString().split('T')[0] %>
            <% } else { %>
              N/A
            <% } %>
      </div>
      <% } else { %>
        <p>No favorite found.</p>
      <% } %>
      <a href ="/" class="btn btn-secondary mt-3">Back</a>
  </div>

</body>
</html>