<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
        <title>Hawker Hero App</title>
    </head>
<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <div class="container-fluid">
          <a class="navbar-brand" href="/">Hawker Hero App</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="collapsibleNavbar">
            <ul class="navbar-nav">
              <li class="nav-item">
                <a class="nav-link" href="/addFavorite">Add new favorite</a>
              </li> 
            </ul>
            <% if (user) { %>
              <div class="ms-auto">
                <a class="btn btn-outline-light" href="/logout">Logout</a>
              </div>
            <% } %>
          </div>
        </div>
    </nav>

    <div class="container">
        <br>
        <div class="text-center"><h2>Update Favorite</h2></div>
        <br>
        <div class="text-center">
          <% if (favorite) { %>
            <form action="/editFavorite/<%= favorite.id %>" method="POST">
                <label for="user_id">Username:</label><br>
                <input type="text" id="username" name="username" value ="<%= favorite.username %>" required><br><br>

                <label for="stall">Stall:</label><br>
                <input type="text" id="stall" name="stall" value ="<%= favorite.stall %>" required><br><br>

                <label for="food">Food:</label><br>
                <input type="text" id="food" name="food" value ="<%= favorite.food %>" required><br><br>

                <label for="notes">Notes:</label><br>
                <input type="text" id="notes" name="notes" value ="<%= favorite.notes %>" required><br><br>

                <button type="submit" class="btn btn-outline-dark">Update Favorite</button>
            </form>
          <% } else { %>
              <p>No favorite found.</p>
          <% } %>
          <a href ="/" class="btn btn-outline-dark mt-3">Back</a>
        </div>
    </div>
</body>
</html>