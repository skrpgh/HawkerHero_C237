<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>HawkerHero</title>
</head>

<body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">HawkerHero</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="collapsibleNavbar">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="/reviewadmin">Review</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/addreviews">Add New Review</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/logout">Logout</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Main content -->
    <div class="container mt-4">
        <h2 class="text-center mb-4">Update Review</h2>

        <% if (reviews) { %>
            <form action="/updatereview/<%= reviews.id %>" method="POST" enctype="multipart/form-data" class="mx-auto"
                style="max-width: 500px;">

                <div class="mb-3">
                    <input type="hidden" name="user_id" value="<%= reviews.user_id %>">
                </div>

                <div class="mb-3">
                    <label for="rating" class="form-label">Rating</label>
                    <select id="rating" name="rating" class="form-control" value="<%= reviews.comment %>" required>
                        <option value="1" <%=reviews.rating==1 ? 'selected' : '' %>>1 star</option>
                        <option value="2" <%=reviews.rating==2 ? 'selected' : '' %>>2 star</option>
                        <option value="3" <%=reviews.rating==3 ? 'selected' : '' %>>3 star</option>
                        <option value="4" <%=reviews.rating==4 ? 'selected' : '' %>>4 star</option>
                        <option value="5" <%=reviews.rating==5 ? 'selected' : '' %>>5 star</option>
                    </select>
                </div>

                <div class="mb-3">
                    <label for="comment" class="form-label">comment</label>
                    <input type="comment" class="form-control" id="comment" name="comment" min="0" step="0.01" value="<%= reviews.comment %>" required>
                </div>

                <div class="mb-3">
                    <label class="form-label">Current Image</label>
                    <img src="/images/<%= reviews.image %>" class="img-thumbnail mt-2";">
                </div>

                <div class="mb-4">
                    <label for="image" class="form-label">Upload New Image</label>
                    <input class="form-control" type="file" id="image" name="image" accept="image/*">
                </div>

                <div class="d-grid gap-2">
                    <button type="submit" class="btn btn-primary">Update Review</button>
                    <a href="/reviewadmin" class="btn btn-secondary">Back to Review</a>
                </div>
            </form>
            <% } else { %>
                <p class="text-center">No preview found.</p>
                <% } %>
    </div>
</body>

</html>